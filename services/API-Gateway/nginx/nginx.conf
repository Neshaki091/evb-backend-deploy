# Định nghĩa Upstream (Giữ nguyên)
upstream auth_service {
    server auth-service:3000;
}
upstream listing_service {
    server listing-service:5000;
}
upstream review_service {
    server review-service:8000;
}
upstream search_service {
    server search-service:8004;
}
upstream transaction_service {
    server transaction-service:3001;
}
upstream reports_service {
    server reports-service:8001;
}
upstream wishlist_service {
    server wishlist-service:8002;
}
upstream analytics_service {
    server analytics-service:8003;
}

server {
    listen 80;

    # ... (CORS & OPTIONS - Giữ nguyên)
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
    add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

    if ($request_method = 'OPTIONS') {
        return 204;
    }
    # ========================================================
    # 2. PROXY PASS ĐƠN GIẢN (KHÔNG DÙNG REWRITE)
    # ========================================================

    # 🔑 AUTH Service: /api/auth/login -> /users/login
    location /api/auth/ {
        # SỬA LỖI: Chỉ định đường dẫn gốc mong muốn của Auth Service
        proxy_pass http://auth_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # 🚗 LISTING Service: /api/listings/* -> /*
    location /api/listings/ {
        # Do listing route thường bắt đầu từ / (ví dụ: /public), ta chỉ định /
        proxy_pass http://listing_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # 💰 TRANSACTION Service (Member Routes): /api/transactions/* -> /orders/*
    location /api/transactions/ {
        # SỬA LỖI: Chỉ định rõ ràng thư mục route /orders/
        proxy_pass http://transaction_service/orders/;
        proxy_set_header Authorization $http_authorization;
    }

    # 🔒 ADMIN TRANSACTION Routes: /api/admin/transactions/* -> /admin/*
    location /api/admin/transactions/ {
        # Chỉ định rõ ràng thư mục route /admin/
        proxy_pass http://transaction_service/admin/;
        proxy_set_header Authorization $http_authorization;
    }
    # Thêm logic Admin Fees trực tiếp (Nếu Transaction Service có endpoint /admin/fees)
    location /api/admin/fees/ {
        proxy_pass http://transaction_service/admin/fees/;
        proxy_set_header Authorization $http_authorization;
    }
    # ----------------------------------------------------
    # REVIEW, WISHLIST, REPORTS, ANALYTICS (Tất cả đều proxy về gốc /)
    # ----------------------------------------------------

    # 💬 REVIEW Service
    location /api/reviews/ {
        proxy_pass http://review_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # ⭐ WISHLIST Service
    location /api/wishlist/ {
        proxy_pass http://wishlist_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # 🚨 REPORTS Service
    location /api/reports/ {
        proxy_pass http://reports_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # 📈 ANALYTICS Service
    location /api/analytics/ {
        proxy_pass http://analytics_service/;
        proxy_set_header Authorization $http_authorization;
    }

    # 🔍 SEARCH Service
    location /api/search/ {
        # Giữ nguyên như cũ, giả định Search Service xử lý path
        proxy_pass http://search_service/;
        proxy_set_header Authorization $http_authorization;
    }
}